//array, object 

//ts - tuple

let bazarList =['eggs', 'milk', 'sugar'];


let mixeArr = ['eggs',12,'milk',1,'sugar',4]

mixeArr.push('appale');

let couple: [string, string] = ["Husband",
    "wife"];

    //ref ? eta dara bujhano hoy optional, thakteo paray nao thakte paray 

   const user: {
    firstName: string;
    middleName?:String; //optional type ? 
     lastName: String;
   } = {
        firstName: 'Sabba',
        // middleName: 'hossain',
        lastName: 'Forhan'
    };

    console.log(user);
    